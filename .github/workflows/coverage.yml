name: Coverage

on:
  push:
    branches: [ main, dev, fw-main ]
  pull_request:
    branches: [ main, dev, fw-main ]
  merge_group:
    branches: [ main, dev, fw-main ]
  workflow_dispatch:
  schedule:
    - cron: '0 8 * * *'  # Daily at midnight PST (8 AM UTC)
env:
  CARGO_TERM_COLOR: always
  CACHE_KEY_ID: 3a7ebbf3b54004f25e780cae3d987f1b4a83d493

jobs:

  coverage_ubuntu:

    env:
      JOB_ID: coverage_ubuntu-azihsm-x64-gh-ubuntu2404-pool-${{ github.run_id }}-${{ github.run_number }}-${{ github.run_attempt }}

    runs-on: [azihsm-x64-gh-ubuntu2404-pool]

    defaults:
      run:
        working-directory: .

    steps:
    - uses: actions/checkout@v6
    - uses: ./.github/actions/ubuntu-setup
    - name: Free disk space
      run: |
        # The two coverage steps below run with --skip-clean, so the
        # instrumented mock-test artifacts stay on disk while the resiliency
        # step builds a second, fully instrumented copy of the native
        # api_tests harness (googletest + cbindgen + a duplicate dependency
        # tree). Together they can exhaust the runner's disk ("No space left
        # on device"). Reclaim ~25 GB by removing large preinstalled
        # toolchains this project does not use.
        echo "Disk space before cleanup:" && df -h /
        sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/.ghcup /usr/local/lib/android /opt/hostedtoolcache/CodeQL
        sudo docker image prune --all --force || true
        echo "Disk space after cleanup:" && df -h /
    - name: Run all mock tests with code coverage
      id: test-mock-coverage
      run: cargo xtask precheck --coverage --skip-clean --exclude provider-integration-tests-cli --exclude provider-integration-tests-capi -F mock --profile ci-mock
      continue-on-error: true
    - name: Run resiliency fault injection tests with code coverage
      id: test-res-fault
      run: cargo xtask precheck --coverage --skip-clean -p azihsm_api_tests -F mock,res-test -E 'test(resiliency::fault_injection::)' --profile ci-mock-res
      continue-on-error: true
    - name: Generate nextest report
      id: nextest-report
      run: cargo xtask precheck --nextest-report
    - name: Code Coverage Summary Report
      if: ${{ steps.test-mock-coverage.outcome == 'success' && steps.test-res-fault.outcome == 'success' }}
      run: cargo xtask precheck --coverage-report
    - name: Upload coverage report
      if: ${{ steps.test-mock-coverage.outcome == 'success' && steps.test-res-fault.outcome == 'success' }}
      uses: actions/upload-artifact@v7
      with:
        name: coverage-report-ubuntu
        path: ./target/reports/
    - name: Fail if any tests failed
      if: ${{ steps.test-mock-coverage.outcome == 'failure' || steps.test-res-fault.outcome == 'failure' }}
      run: |
        echo "One or more tests failed."
        exit 1

  coverage_windows:

    env:
      JOB_ID: coverage_windows-azihsm-x64-gh-winserver22-pool-${{ github.run_id }}-${{ github.run_number }}-${{ github.run_attempt }}

    runs-on: [azihsm-x64-gh-winserver22-pool]

    defaults:
      run:
        working-directory: .

    steps:
    - uses: actions/checkout@v6
    - uses: ./.github/actions/windows-setup
    - name: Run all mock tests with code coverage
      id: test-mock-coverage
      run: cargo xtask precheck --coverage --skip-clean --exclude provider-integration-tests-cli --exclude provider-integration-tests-capi -F mock --profile ci-mock
      continue-on-error: true
    - name: Run resiliency fault injection tests with code coverage
      id: test-res-fault
      run: cargo xtask precheck --coverage --skip-clean -p azihsm_api_tests -F mock,res-test -E 'test(resiliency::fault_injection::)' --profile ci-mock-res
      continue-on-error: true
    - name: Generate nextest report
      id: nextest-report
      run: cargo xtask precheck --nextest-report
    - name: Code Coverage Summary Report
      if: ${{ steps.test-mock-coverage.outcome == 'success' && steps.test-res-fault.outcome == 'success' }}
      run: cargo xtask precheck --coverage-report
    - name: Upload coverage report
      if: ${{ steps.test-mock-coverage.outcome == 'success' && steps.test-res-fault.outcome == 'success' }}
      uses: actions/upload-artifact@v7
      with:
        name: coverage-report-windows
        path: ./target/reports/
    - name: Fail if any tests failed
      if: ${{ steps.test-mock-coverage.outcome == 'failure' || steps.test-res-fault.outcome == 'failure' }}
      run: |
        echo "One or more tests failed in this job."
        exit 1

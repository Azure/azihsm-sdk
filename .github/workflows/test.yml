name: Test

on:
  push:
    branches: [ main, dev, fw-main ]
  pull_request:
    branches: [ main, dev, fw-main ]
  merge_group:
    branches: [ main, dev, fw-main ]
  workflow_dispatch:

jobs:
  simple_ubuntu:

    runs-on: [azihsm-arm64-gh-westus2-pool]

    defaults:
      run:
        working-directory: .

    steps:
        - name: echo
          run: echo "Hello world"